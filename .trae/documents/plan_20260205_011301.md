I will implement the requested `StopDisplayCard` component to meet all design and technical specifications.

**Plan:**

1.  **Create `src/components/StopDisplayCard.jsx`**:
    *   **Structure**: A vertical card container (`flex-col`) with `rounded-xl`, `shadow-sm`, and `overflow-hidden`.
    *   **Carousel (Top 40%)**:
        *   Implement a swipeable image area using CSS scroll snap (`overflow-x-auto snap-x snap-mandatory`).
        *   Add dot indicators overlay at the bottom of the image area.
        *   Use `aspect-[4/3]` or specific height calculation to ensure it takes up ~40% of the card or a fixed responsive height suitable for mobile.
    *   **Content (Bottom)**:
        *   **Typography**: Use `text-lg` (18px+) for price and `text-sm` (14px+) for body text.
        *   **Price**: Large, bold text with the currency symbol (e.g., `₹500`).
        *   **Description**: Truncated text (`line-clamp-3`) with `leading-relaxed` (1.5x line height).
    *   **Action Button**:
        *   A circular Floating Action Button (FAB) at the bottom-right intersection of the image and content or floating over the content.
        *   Styled with `bg-primary`, `rounded-full`, `shadow-lg`, and a ripple effect on click.

2.  **Integrate into `StopsManagement.jsx`**:
    *   Replace the current simple `div` list of stops with a grid of `StopDisplayCard` components.
    *   Pass necessary props: `stop` data (name, price, description, images) and `onAdd` handler.

3.  **Responsiveness & Accessibility**:
    *   Ensure the card is 100% width on mobile (`w-full`) with `mx-4` (16px margins).
    *   Add `aria-label` to buttons and `alt` text to images.
    *   Verify touch target sizes (min 44px) for the add button.

This component will serve as the user-facing "Stop" card, distinct from the administrative `StopCard` used for editing.