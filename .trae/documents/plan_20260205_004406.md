The user is facing two issues:
1.  **Stops not appearing**: This is because `AddRouteForm.jsx` tries to call `routeService.getStopImages()`, but this method is **missing** in `routeService.js`. This causes the fetch to crash silently (caught by a try-catch block), leaving the stops list empty.
2.  **`net::ERR_UNKNOWN_URL_SCHEME`**: This is caused by `mockDb.js`'s `rehydrateImages` function returning raw `idb:` strings when an image is missing from IndexedDB. The browser then tries to render `<img src="idb:..." />`, causing the network error.

**Plan:**
1.  **Update `src/services/routeService.js`**: Add the missing `getStopImages` method. This will fix the crash in `AddRouteForm.jsx` and allow stops to load.
2.  **Update `src/services/mockDb.js`**: Modify `rehydrateImages` to return `null` (or a placeholder) instead of the raw `idb:` string if the image blob is not found. This will prevent the `ERR_UNKNOWN_URL_SCHEME` error in the browser console.
3.  **Update `src/services/mockDb.js`**: Add error handling to `getStopImages` (similar to `getStopsByRouteId`) to ensure it doesn't crash if hydration fails.

This two-pronged approach ensures stops load correctly and missing images are handled gracefully.