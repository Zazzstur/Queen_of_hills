I will convert this static Vite project into a full-stack Cloudflare Pages application as requested.

**Plan:**

1. **Configure Wrangler**:

   * Delete the existing `wrangler.json` (since `wrangler.toml` is requested/standard for this setup).

   * Create a `wrangler.toml` file configured for Cloudflare Pages, pointing to the `dist` directory and setting compatibility dates.

2. **Set up Cloudflare Functions**:

   * Create a `/functions` directory in the project root.

   * Create `/functions/api/[[route]].js` to handle API requests. This file will:

     * Initialize a Supabase client using Cloudflare's `env` context (server-side).

     * Proxy requests to Supabase or handle custom logic securely.

   * Ensure the client-side code still works for static assets (Cloudflare Pages handles this automatically by serving `dist` for non-function routes).

3. **Refactor Client-Side Supabase (Optional but Recommended)**:

   * The current client-side `supabase.js` uses `import.meta.env`. This is correct for a Vite SPA and **should remain** for client-side interactions (like Auth or direct DB access if RLS is enabled).

   * **However**, for the new server-side functions in `/functions`, we will create a separate initialization logic that uses `context.env` instead of `import.meta.env`.

4. **Migration Strategy**:

   * We will keep the existing client-side logic working.

   * We will add the `/functions` layer to enable server-side capabilities (like secure API endpoints that don't expose keys, though Supabase Anon keys are safe to expose).

   * If specific API endpoints need to be moved to the server (e.g., for admin actions), we will create those in `/functions/api/`.

5. **Environment Variables**:

   * I will update `DEPLOYMENT_GUIDE.md` to explain that `VITE_*` variables are for the client (build time) and `SUPABASE_*` variables (without VITE prefix) should be added to Cloudflare for the server-side functions.

**Clarification**: The user asked to "Refactor all Supabase client initialization code". In a Cloudflare Pages Full Stack app, you typically have **two** clients:

1. **Client-side (React)**: Uses `VITE_SUPABASE_ANON_KEY` (public).
2. **Server-side (Functions)**: Uses `SUPABASE_SERVICE_ROLE_KEY` (private) or Anon key, accessed via `context.env`.

I will implement the structure to support both, ensuring the `functions` directory is set up correctly.

**Action Items:**

1. Delete `wrangler.json`.
2. Create `wrangler.toml`.
3. Create `functions/api/test.js` (to verify server-side setup).
4. Create `functions/_middleware.js` (for error handling/CORS).
5. Update `package.json` scripts if necessary.

